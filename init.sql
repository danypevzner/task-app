-- Создаем схему, если нет
CREATE SCHEMA IF NOT EXISTS kanban;

-- Пример таблицы (если Hibernate не создает сам, или для начальных данных)
-- Обычно Hibernate с ddl-auto=update/create делает это сам, но для чистоты можно явно.
-- Для валидации (validate) таблицы должны быть созданы заранее через миграции!